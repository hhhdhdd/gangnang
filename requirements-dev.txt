# Dev / test-only dependencies (not needed at runtime on Railway).
# Run: pip install -r requirements.txt -r requirements-dev.txt && pytest
-r requirements.txt
pytest==8.3.3
pytest-asyncio==0.24.0
aiosqlite==0.20.0
